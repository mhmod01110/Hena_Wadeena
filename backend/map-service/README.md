# Hena Wadeena Map Service

- Port: `8003`
- Database env: `MAP_DATABASE_URL`
- API prefixes: `/api/v1/map`, `/api/v1/carpool`
- Resource path: `/pois`

Endpoints (for each prefix):
- `POST /pois`
- `GET /pois`
- `GET /pois/{id}`
- `PATCH /pois/{id}`
