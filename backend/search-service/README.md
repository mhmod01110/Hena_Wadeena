# Hena Wadeena Search Service

- Port: `8009`
- Database env: `SEARCH_DATABASE_URL`
- API prefixes: `/api/v1/search`
- Resource path: `/documents`

Endpoints (for each prefix):
- `POST /documents`
- `GET /documents`
- `GET /documents/{id}`
- `PATCH /documents/{id}`
