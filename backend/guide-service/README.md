# Hena Wadeena Guide Service

- Port: `8005`
- Database env: `GUIDE_DATABASE_URL`
- API prefixes: `/api/v1/guides`, `/api/v1/bookings`
- Resource path: `/profiles`

Endpoints (for each prefix):
- `POST /profiles`
- `GET /profiles`
- `GET /profiles/{id}`
- `PATCH /profiles/{id}`
